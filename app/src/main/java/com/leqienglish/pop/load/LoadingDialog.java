package com.leqienglish.pop.load;

import android.content.Context;

import com.leqienglish.pop.CustomDialog;

public class LoadingDialog extends CustomDialog {
    public LoadingDialog(Context context) {
        super(context);
    }
}
