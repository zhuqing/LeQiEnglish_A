<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="downloadProgressBar">
        <attr name="dptextsize" format="dimension"/>
    </declare-styleable>

    <!--name为声明的"属性集合"名，可以随便取，但是最好是设置为跟我们的View一样的名称-->
    <declare-styleable name="RoundImageView">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="roundRadius" format="integer"></attr>
        <attr name="type" format="integer"></attr>
    </declare-styleable>

    <declare-styleable name="CirclePrecentView">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="background_color" format="color"></attr>
        <attr name="front_color" format="color"></attr>
        <attr name="precent" format="float"></attr>
    </declare-styleable>
    <declare-styleable name="OperaiontBar">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->

        <attr name="items" format="string"></attr>
    </declare-styleable>
    <declare-styleable name="OperaiontBarItem">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="image" format="reference"></attr>
        <attr name="title" format="string"></attr>
    </declare-styleable>


    <!--
        max                 最大进度
        progress            初始时的进度
        startAngle             进度从圆环的哪个角度开始走
        centreColor         圆环内部的填充色
        ringColor             圆环的颜色
        ringProgressColor   圆环进度的颜色
        ringWidth             圆环宽度
        textColor             中心文字的颜色
        textSize             中心文字的大小
        textIsDisplayable     中心文字是否显示
        style = 0/1          进度的风格，实心或者空心
     -->
    <declare-styleable name="RoundProgressBar">
        <attr name="max" format="integer"/>
        <attr name="progress" format="integer"/>
        <attr name="startAngle" format="integer"/>
        <attr name="ringColor" format="color"/>
        <attr name="ringProgressColor" format="color"/>
        <attr name="ringWidth" format="dimension"/>
        <attr name="centerColor" format="color"/>
        <attr name="textColor" format="color"/>
        <attr name="textSize" format="dimension"/>
        <attr name="textIsDisplayable" format="boolean"/>
        <attr name="style">
            <enum name="STROKE" value="0"/>
            <enum name="FILL"   value="1"/>
        </attr>
    </declare-styleable>

</resources>